export interface EachMyRequestFetchType {
  id: number;
  resource: string;
  item: string;
  access: string;
  status: string;
  remarks: string;
  reason_for_access: string;
}

export interface FormDataRequestType {
  request_id: string;
}
