import React from 'react';
import { storiesOf } from '@storybook/react';

import SearchBar from '.';

storiesOf('Common component Guide', module).add('SearchBar component', () => (
  <SearchBar />
));
